# Production Configuration (EC2/Docker Proxy)
VITE_BACKEND_URL=/api
VITE_WS_URL=/ws

# For Local Development (ONLY if not using Docker):
# VITE_BACKEND_URL=http://localhost:5000/api
# VITE_WS_URL=ws://localhost:5000
