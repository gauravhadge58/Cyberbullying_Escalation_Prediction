server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to the backend if needed (optional if frontend uses env URL)
    # But usually, it's safer to just let the browser hit the backend URL directly.
}
